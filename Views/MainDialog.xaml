<Window x:Class="JSE_RevitAddin_MEP_OPENINGS.Views.MainDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JSE_RevitAddin_MEP_OPENINGS.Views"
        Title="JSE MEP Openings - Manager"
        Width="1400" Height="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ConVoidStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2E3440" Padding="15">
            <DockPanel>
                <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                    <TextBlock Text="JSE MEP Openings" Foreground="White" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="{Binding ProfileSummary}" Foreground="#D8DEE9" FontSize="12"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding StatusText}" Foreground="#D8DEE9" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Content: Three panels -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <local:LeftPanel Grid.Column="0" DataContext="{Binding LeftPanelViewModel}"/>
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#4C566A"/>
            <local:MiddlePanel Grid.Column="2" DataContext="{Binding MiddlePanelViewModel}"/>
            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="#4C566A"/>
            <local:RightPanel Grid.Column="4" DataContext="{Binding RightPanelViewModel}"/>
        </Grid>

        <!-- Bottom Toolbar -->
        <Border Grid.Row="2" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0">
            <Grid Margin="15,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Style="{StaticResource IconButtonStyle}" Content="←" ToolTip="Previous"/>
                    <Button Style="{StaticResource IconButtonStyle}" Content="→" ToolTip="Next" Margin="5,0,0,0"/>
                    <Button Style="{StaticResource IconButtonStyle}" Content="⚡" ToolTip="Process" Margin="5,0,0,0"/>
                    <Button Style="{StaticResource IconButtonStyle}" Content="🔧" ToolTip="Settings" Margin="5,0,0,0"/>
                    <Button Style="{StaticResource IconButtonStyle}" Content="🔄" ToolTip="Refresh" Margin="5,0,0,0"/>
                    <ComboBox Width="160" Margin="10,0,0,0" ItemsSource="{Binding OpeningModes}" SelectedItem="{Binding SelectedOpeningMode}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <ProgressBar Width="220" Height="20" Margin="0,0,10,0" Maximum="100" Value="{Binding ProgressPercentage}"/>
                    <TextBlock Text="{Binding ProgressText}" VerticalAlignment="Center" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="OK" Width="90" Height="28" Margin="0,0,10,0" Command="{Binding OkCommand}"/>
                    <Button Content="Cancel" Width="90" Height="28" Margin="0,0,10,0" Command="{Binding CancelCommand}"/>
                    <Button Content="Save" Width="90" Height="28" Command="{Binding SaveCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>