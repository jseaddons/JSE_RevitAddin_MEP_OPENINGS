<Window x:Class="JSE_RevitAddin_MEP_OPENINGS.Views.ProfileSetupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JSE_RevitAddin_MEP_OPENINGS.Views.Converters"
        mc:Ignorable="d"
        Title="JSE MEP Openings - Profile Setup"
        Width="520" Height="720" MinWidth="500" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F8F9FA">
    
    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding CreateProfileCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}"/>
    </Window.InputBindings>
    
    <Window.Resources>
        <!-- Converters -->
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        
        <!-- Button Styles -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004578"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#007ACC"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GroupBox Style -->
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Border Background="White" 
                                    BorderBrush="#DEE2E6" 
                                    BorderThickness="1" 
                                    CornerRadius="5" 
                                    Padding="15">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <TextBlock Grid.Row="0" 
                                               Text="{TemplateBinding Header}" 
                                               FontWeight="Bold" 
                                               FontSize="14" 
                                               Foreground="#2C3E50" 
                                               Margin="0,0,0,10"/>
                                    
                                    <ContentPresenter Grid.Row="1" 
                                                      Content="{TemplateBinding Content}"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- RadioButton Style -->
        <Style TargetType="RadioButton">
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="*"/>     <!-- Scrollable Content -->
            <RowDefinition Height="Auto"/>  <!-- Action Buttons -->
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Profile Setup" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- Scrollable middle content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Profile Name -->
                <GroupBox Header="User Profile">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Profile Name*:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="12" Foreground="#2C3E50"/>
                        <TextBox Grid.Column="1" Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}" Tag="Enter your profile name (e.g., John_Architect)"/>
                    </Grid>
                </GroupBox>

                <!-- Primary + MEP Discipline Selection -->
                <GroupBox>
                    <GroupBox.Header>
                        <DockPanel>
                            <TextBlock Text="Discipline Selection*" DockPanel.Dock="Left"/>
                            <Button Content="Clear all" DockPanel.Dock="Right" 
                                    Command="{Binding ClearAllCommand}"
                                    Background="Transparent" BorderThickness="0" Foreground="#007ACC" Cursor="Hand"/>
                        </DockPanel>
                    </GroupBox.Header>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <!-- Primary Disciplines (Single Selection) -->
                        <TextBlock Grid.Row="0" Text="Select one:" FontWeight="Bold" Margin="0,0,0,10" FontSize="12" Foreground="#2C3E50"/>
                        <StackPanel Grid.Row="1" Margin="0,0,0,15" IsEnabled="{Binding ArePrimaryEnabled}">
                            <RadioButton Content="Architectural" GroupName="PrimaryDiscipline" IsChecked="{Binding IsArchitecturalSelected}"/>
                            <RadioButton Content="Structural" GroupName="PrimaryDiscipline" IsChecked="{Binding IsStructuralSelected}"/>
                            <RadioButton Content="Technical Planner" GroupName="PrimaryDiscipline" IsChecked="{Binding IsTechnicalPlannerSelected}"/>
                            <RadioButton Content="Coordination" GroupName="PrimaryDiscipline" IsChecked="{Binding IsCoordinationSelected}"/>
                        </StackPanel>
                        <!-- MEP Disciplines (Multi Selection) -->
                        <TextBlock Grid.Row="2" Text="MEP Disciplines (Can Select Multiple):" FontWeight="Bold" Margin="0,0,0,10" FontSize="12" Foreground="#2C3E50"/>
                        <StackPanel Grid.Row="3" IsEnabled="{Binding AreMepEnabled}">
                            <CheckBox Content="Mechanical" IsChecked="{Binding IsMechanicalSelected}"/>
                            <CheckBox Content="Plumbing" IsChecked="{Binding IsPlumbingSelected}"/>
                            <CheckBox Content="Fire Fighting" IsChecked="{Binding IsFireProtectionSelected}"/>
                            <CheckBox Content="Electrical" IsChecked="{Binding IsElectricalSelected}"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Warning Message -->
                <Border Background="#FFF3CD" BorderBrush="#FFEAA7" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15">
                    <TextBlock Text="⚠️ You should not change the Settings in an ongoing project because your old initials will remain in the void elements." FontSize="11" TextWrapping="Wrap" Foreground="#856404"/>
                </Border>

                <!-- Validation Message -->
                <Border Background="#F8D7DA" BorderBrush="#F5C6CB" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15" Visibility="{Binding ValidationMessage, Converter={StaticResource StringToVisibilityConverter}}">
                    <TextBlock Text="{Binding ValidationMessage}" FontSize="11" TextWrapping="Wrap" Foreground="#721C24"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
        
        <!-- Visible action buttons + keyboard shortcuts -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <StackPanel.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding CreateProfileCommand}"/>
                <KeyBinding Key="Escape" Command="{Binding CancelCommand}"/>
            </StackPanel.InputBindings>
            <Button Content="Continue" 
                    Command="{Binding CreateProfileCommand}" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Width="100"
                    Margin="0,0,10,0"/>
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}" 
                    Style="{StaticResource SecondaryButtonStyle}" 
                    Width="90"/>
        </StackPanel>
    </Grid>
</Window>
