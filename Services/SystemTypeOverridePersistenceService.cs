using System;
using System.IO;

namespace JSE_RevitAddin_MEP_OPENINGS.Services
{
    /// <summary>
    /// DEPRECATED: Handles persistence of System Type Override settings.
    /// Logic stubbed out to remove dependency on MarkPrefixSettings.
    /// </summary>
    public static class SystemTypeOverridePersistenceService
    {
        // Stubbed to return a plain object
        public static object LoadSettings()
        {
            return new object();
        }

        // Stubbed to accept object and do nothing
        public static void SaveSettings(object settings)
        {
            // No-op
        }
    }
}
